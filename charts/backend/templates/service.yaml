apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-dogops-backend-chart.fullname" . }}
  labels:
    {{- include "devops-dogops-backend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-dogops-backend-chart.selectorLabels" . | nindent 4 }}
